# Sesion 2026-06-13
[[masgesth]]

## Foco

- cerrar la ventana de upgrade de `MySQL 8.0.44` a `MySQL 8.4.9`;
- validar arranque real de la app tras el cambio de servidor;
- documentar las incidencias de infraestructura para no redescubrirlas.

## Cambios cerrados

- completado el salto de servidor a `MySQL 8.4.9`;
- el instalador MSI dejo binarios `8.4` en paralelo, pero `MySQL Configurator`
  no detecto la instancia actual como `in-place upgrade` y la trato como
  configuracion nueva;
- se resolvio reapuntando manualmente el servicio `MySQL80` al binario:
  - `C:\Program Files\MySQL\MySQL Server 8.4\bin\mysqld.exe`
  - manteniendo `my.ini` y `datadir` de
    `C:\ProgramData\MySQL\MySQL Server 8.0`;
- `SELECT VERSION();` confirmado en `8.4.9`.

## Incidencia principal encontrada

- tras el upgrade, la app no arrancaba y devolvia:
  - `1524 (HY000): Plugin 'mysql_native_password' is not loaded`;
- diagnostico:
  - la app usa `mysql-connector-python 8.4.0`;
  - el problema no estaba en el conector, sino en el usuario de aplicacion;
  - `administrador@%` seguia configurado con `mysql_native_password`;
- resolucion:
  - migrado `administrador@%` a `caching_sha2_password`;
  - resultado: la app vuelve a abrir y conectar correctamente.

## Validaciones realizadas

- backup logico ya registrado previamente y mantenido como evidencia;
- restauracion operativa de las bases contrastada varias veces por usuario;
- `SELECT VERSION();` devuelve `8.4.9`;
- arranque correcto de la app tras cambiar el plugin de autenticacion del
  usuario de aplicacion.

## Riesgos o notas

- `MySQL Workbench 8.0.44` muestra warning de compatibilidad al conectar contra
  `MySQL 8.4.9`, pero permite continuar;
- queda recomendado actualizar Workbench en otra ventana tranquila de
  mantenimiento;
- leccion operativa importante:
  - al subir a `MySQL 8.4+`, revisar siempre el `plugin` de los usuarios de
    aplicacion antes de dar el cambio por cerrado;
  - si el configurador no detecta la instancia existente, no continuar como
    instalacion nueva sin revisar antes servicio, `my.ini` y `datadir`.

## Siguiente paso recomendado

- volver al roadmap funcional y retomar `roles/auditoria`;
- no abrir mas cambios de infraestructura salvo necesidad real;
- mantener esta sesion como referencia de upgrade en Windows para futuras
  actualizaciones.

## Segunda sesion del dia

### Foco

- retomar el frente funcional de `roles/permisos`;
- completar el primer corte operativo de `usuario -> rol -> permisos` en
  `Gestion`;
- dejar trazado un pendiente transversal de ergonomia sobre ventanas duplicadas.

### Cambios cerrados

- reorganizada la capa SQL para separar:
  - `bd/base/` como esquema de referencia;
  - `bd/migraciones/` como scripts incrementales ejecutables;
  - `scripts/db/` como automatizacion operativa;
- documentado el modelo objetivo en
  `docs/propuesta_modelo_roles_permisos.md`;
- sembrado y validado el modelo base de:
  - `roles`;
  - `permisos`;
  - `roles_permisos`;
  - `usuarios.id_rol`;
- mantenida compatibilidad transitoria con `usuarios.rol` y `es_tecnico`;
- implantado helper comun de autorizacion en
  `src/compartido/autorizacion.py`;
- endurecido el acceso visible y funcional por permisos en `Gestion`:
  - `Usuarios`;
  - `Centros`;
  - `Equipos`;
  - `Compras`;
  - `Estadisticas`;
  - `Proveedores`;
  - `Repuestos`;
  - `Marcas`;
  - `Tipos trabajo`;
- creada la nueva pantalla `Roles` con:
  - listado;
  - alta y modificacion;
  - desactivacion de roles no reservados;
  - permisos agrupados por modulo mediante `checkbox`;
  - scroll vertical por pestana;
  - botonera fija;
  - compactacion horizontal final tras validacion manual de usuario;
- ajustado `usuarios_service` para que los roles dinamicos no se normalicen a
  los roles fijos legacy y respeten `tecnico_campo`.

### Validaciones realizadas

- ejecucion manual por usuario de scripts SQL base y de backfill;
- comprobacion visual de visibilidad de `Usuarios` segun el rol logado;
- comprobacion funcional de `consulta`, `tecnico`, `administrativo` y
  `superusuario` en el menu de `Gestion`;
- creacion manual de rol nuevo desde la UI ya operativa;
- `python -m py_compile` correcto en:
  - `src/compartido/autorizacion.py`;
  - `src/Compras/services/usuarios_service.py`;
  - `src/Compras/services/roles_service.py`;
  - `src/Compras/ui/app.py`;
  - `src/Compras/ui/usuarios_ui.py`;
  - `src/Compras/ui/roles_ui.py`;
  - `src/Compras/ui/cecos_ui.py`;
  - `src/Compras/ui/compras_ui.py`;
  - `src/Compras/ui/dashboard_ui.py`;
  - `src/Compras/ui/equipos_ui.py`;
  - `src/Compras/ui/proveedores_ui.py`;
  - `src/Compras/ui/repuestos_ui.py`;
  - `src/Compras/ui/marcas_ui.py`;
  - `src/Compras/ui/tipos_trabajo_ui.py`.

### Riesgos o notas

- `Compras` queda protegida por permiso, pero sigue aparcada funcionalmente por
  decision operativa de esta sesion;
- los roles dinamicos ya son utilizables, pero falta seguir extendiendo la
  matriz real de permisos a `Preventivos` y `Quirofanos`;
- pendiente transversal documentado para futura tanda:
  - evitar abrir varias instancias de la misma ventana si ya existe una abierta
    en la aplicacion.

### Siguiente paso recomendado

- validar con un usuario real el rol nuevo creado y confirmar que solo expone
  las funcionalidades marcadas;
- abrir en otra sesion una correccion transversal para ventana unica por modulo
  o pantalla;
- continuar despues con la extension controlada del modelo de permisos en
  `Preventivos` y `Quirofanos`.
