# Sesion 2026-06-14
[[masgesth]]

## Foco

- consolidar el resultado del smoke test manual de permisos y ventana unica;
- decidir si el siguiente frente ya puede pasar a `auditoria`;
- dejar trazado el unico hueco pequeno detectado para no perderlo.

## Cambios cerrados

- revisada la evidencia manual anotada en
  `docs/checklist_validacion_hardening_permisos_ventanas.md`;
- confirmada validacion manual base del hardening transversal en:
  - `Gestion`;
  - `Preventivos`;
  - `Quirofanos`;
  - regla de ventana unica en pantallas principales cubiertas;
- confirmada tambien la correccion de foco en import/export de `Quirofanos`;
- anadido indicador visual `Modo lectura` en las cabeceras de:
  - `Informe preventivo`;
  - `Informes Quirofanos`;
- actualizada la documentacion viva para mover la siguiente tarea recomendada a
  `auditoria`;
- preparado el plan de ataque inicial de `auditoria` con este orden:
  - primer corte recomendado sobre `import/export` de campo;
  - segundo corte natural con consulta minima desde UI;
  - tercer corte opcional con exportacion CSV.
- implementado en codigo el primer corte de `auditoria` sobre trabajo de
  campo:
  - `Preventivos`: `exportar_tablet` e `importar_tablet`;
  - `Quirofanos`: `exportar_android` e `importar_android`;
  - trazabilidad en `ok/parcial/error` con detalle resumido por operacion;
  - el detalle persiste la ruta y el resumen operativo, mientras `usuario` y
    `fecha_hora` quedan en las columnas nativas de `auditoria_eventos`.
- implementada la consulta minima de `auditoria` en `Gestion/Seguridad` con:
  - filtros `De/A`;
  - filtro por modulo;
  - filtro por familia de accion:
    - `Importar/Exportar`;
    - `Gestion informes`;
  - scroll horizontal;
  - emergente de detalle;
- implementada primera exportacion CSV desde `Imprimir`, respetando filtros
  activos y con salida en `output/auditoria`.
- endurecido el mantenimiento de `Usuarios` para aislar `superusuario`:
  - solo otro `superusuario` puede asignarlo o retirarlo;
  - solo otro `superusuario` puede modificar o desactivar una cuenta que ya lo
    tenga;
  - la restriccion se aplica en UI y tambien en servicio con trazabilidad de
    bloqueo en auditoria.
- endurecido el `login/acceso` en primer corte productivo:
  - eliminado el acceso implicito al primer usuario activo cuando no hay
    contrasenas;
  - sustituido por bootstrap guiado y acotado:
    - solo sobre cuentas `superusuario` si existen;
    - o sobre usuario activo unico con promocion a `superusuario` cuando el
      entorno lo soporta;
    - y bloqueo expreso si hay varios usuarios activos sin candidato seguro;
  - el bootstrap ya obliga a fijar contrasena inicial antes de abrir sesion;
  - el login normal ya aplica bloqueo temporal en memoria de proceso tras 3
    intentos fallidos sobre la misma cuenta;
  - anadida trazabilidad de:
    - `bootstrap_password` o `bootstrap_superusuario`;
    - `login_bootstrap`;
    - `login_bloqueado_intentos`.
- regularizado el estado real de la base tras pruebas manuales:
  - se detecto que la tabla `usuarios` restaurada habia dejado la cuenta activa
    sin `id_rol`, por eso la app la interpretaba como `Tecnico`;
  - la base queda ya saneada con una sola cuenta activa `superusuario`, con
    contrasena y copia de seguridad hecha;
  - retirado el fallback mas laxo que abria `Gestion` por nombre de rol alto;
  - se mantiene la compatibilidad robusta por resolucion de rol y la
    regularizacion segura de cuenta unica.

## Validaciones realizadas

- perfiles probados segun la checklist:
  - rol amplio o completo;
  - rol reducido a `Quirofanos` con visualizacion de informes;
  - rol reducido a `Preventivos` con acceso de visualizacion;
  - rol reducido a ver e imprimir informes;
- resultado funcional:
  - permisos visibles correctos en los flujos probados;
  - denegacion funcional correcta en acciones bloqueadas;
  - no duplicidad de ventanas en la segunda apertura;
  - recuperacion de foco correcta en las pantallas cubiertas.
- validacion tecnica del primer corte de auditoria:
  - `python -m py_compile` correcto en los cuatro servicios tocados de
    import/export.
- validacion visual funcional del flujo ya completada en las 4 operativas de
  import/export.
- validacion funcional/SQL del primer corte de auditoria ya completada en
  `gestion.auditoria_eventos`:
  - `Preventivos/exportar_tablet`;
  - `Preventivos/importar_tablet`;
  - `Quirofanos/exportar_android`;
  - `Quirofanos/importar_android`.
- validacion tecnica del primer corte de `login/acceso`:
  - `python -m compileall main.py src` correcto;
  - `python -m py_compile src/Compras/ui/login_ui.py src/Compras/services/usuarios_service.py src/compartido/auth.py src/compartido/auditoria_service.py`
    correcto.
- validacion manual posterior del login ya completada:
  - el `superusuario` entra y vuelve a ver todos los modulos;
  - un usuario de prueba queda bloqueado durante 5 minutos tras 3 intentos
    fallidos;
  - en auditoria aparecen los 3 intentos fallidos y el bloqueo temporal;
  - tras esperar el tiempo de bloqueo, el usuario vuelve a entrar sin problema
    con la contrasena correcta.

## Riesgos o notas

- no aparece bloqueo funcional nuevo que justifique otra tanda correctiva
  urgente sobre permisos o ventanas;
- no se considera necesario, por ahora, anadir mensaje adicional por modulos no
  visibles mientras la portada ya oculte correctamente lo no permitido.
- la auditoria de campo queda ya implementada y verificada en sus cuatro
  operativas minimas;
- la consulta minima y su CSV ya pueden darse por suficientemente cerrados en
  primera aproximacion tras validacion funcional del usuario;
- la consulta minima ya puede separarse mejor con un segundo filtro por familia
  de accion para no mezclar `Importar/Exportar` con `Gestion informes` al
  revisar o exportar CSV;
- detectada y corregida una posible brecha en `Usuarios` sobre el rol
  `superusuario`;
- no parece necesario afinar mas en esta misma sesion salvo que la validacion
  manual del CSV o de la UI revele una necesidad concreta;
- queda anotado como siguiente deuda potencial de seguridad un futuro sistema
  configurable de avisos por correo, con destino parametrizable y primer caso
  de uso orientado a series repetidas de intentos de acceso fallidos.
- el bloqueo de intentos fallidos queda solo en memoria del proceso actual:
  - sirve como primer corte de hardening;
  - no sustituye una futura politica persistente si el piloto acaba
    requiriendola.
- queda pendiente solo la validacion manual especifica del flujo de bootstrap
  en una base de prueba aislada.
- la incidencia de permisos observada durante esta sesion no venia del modelo
  de UI sino del estado legacy importado de `usuarios`.

## Siguiente paso recomendado

- asumir ya cerrada la primera aproximacion de `auditoria` minima y de su CSV;
- tomar como ya implementado el primer corte tecnico de `login/acceso`;
- asumir ya validado en la base buena:
  - acceso correcto de `superusuario`;
  - bloqueo temporal por intentos;
  - trazabilidad en auditoria;
- ejecutar solo la validacion manual especifica del bootstrap guiado en una
  base de prueba aislada;
- decidir si hace falta un segundo corte con persistencia mayor de intentos o si
  basta documentar instalacion y bootstrap seguro;
- mantener los avisos por correo solo como diseno documentado, no como cambio
  inmediato.
